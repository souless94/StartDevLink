'use client'
import { AppNavBar } from "../_components/AppNavBar";

export default function DocsBoard() {

    return (
        <div>
            <AppNavBar />
            <p>Docs in progress</p>
        </div>
    );
}